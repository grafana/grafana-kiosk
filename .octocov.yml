coverage:
  badge:
    path: docs/coverage.svg
  paths:
    - coverage.out
comment:
  enable: false
codeToTestRatio:
  badge:
    path: docs/ratio.svg
  code:
    - '**/*.go'
    - '!**/*_test.go'
  test:
    - '**/*_test.go'
testExecutionTime:
  if: is_pull_request
  badge:
    path: docs/time.svg
body:
  enable: true
summary:
  if: true
